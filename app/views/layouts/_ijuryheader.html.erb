<header>
<%= link_to image_tag "iJurylogo_oneliner.png", {height: 100, action: 'root_url', alt: "iJury"} %>

<nav>
	<ul class="nav-login">
		<% if session[:id] %>
			<%= button_to "log out", session_path(session[:id]), method: :delete, redirect_to: "/" %>

      <h3>Dashboard for <%= User.find(session[:id]).user_name %></h3>

    <% end %>

</nav>

</header>