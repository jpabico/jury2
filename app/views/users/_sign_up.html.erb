
  <%# form_for :user,  url: users_path do |user_form| %>

  <!-- we passed @users as a local from the main.html.erb file which originated from the controller -->
  <%= form_tag users_path do %>
    <%= text_field_tag :user_name , nil , :placeholder => "e.g. papajons"%>
    <%= password_field_tag :password , nil , :placeholder => "password"%>
    <%= email_field_tag :email , nil , :placeholder => "e.g. sample@example.com"%>
    <%= submit_tag "Sign up" %>
  <% end %>

